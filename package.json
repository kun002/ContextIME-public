{
  "name": "context-ime",
  "version": "0.5.0",
  "private": true,
  "description": "Context-aware bilingual input-mode control layer built to integrate with existing IME and Tree-sitter runtimes.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false",
    "test": "node --import tsx --test packages/policy-core/test/*.test.ts packages/windows-runtime/test/*.test.ts packages/syntax-runtime/test/*.test.ts packages/vscode-adapter/test/*.test.ts",
    "check": "npm run typecheck && npm test",
    "package:vsix": "node scripts/package-extension.mjs"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "@types/vscode": "1.125.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "0.28.2",
    "tsx": "4.23.1",
    "typescript": "5.9.3"
  }
}
